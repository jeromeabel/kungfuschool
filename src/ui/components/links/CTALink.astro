---
import Icon from 'astro-icon';

interface Props {
	label: string;
	href: string;
	outline?: boolean;
	rounded?: boolean;
	icon?: string;
}

const { label, href, outline = false, rounded = false, icon } = Astro.props;

const cssOutline = outline
	? 'bg-black text-white hover:bg-white hover:text-black'
	: 'bg-white text-black hover:bg-black hover:text-white';
---

<a
	class:list={[
		{ 'rounded-full': rounded },
		{ 'flex items-center gap-2': icon },
		`w-fit whitespace-nowrap break-keep border border-white px-4 py-2 text-lg transition-all lg:px-6 ${cssOutline}`,
	]}
	href={href}
	aria-label={`${label}`}
	title={`${label}`}
>
	{label}
	{icon ? <Icon size={24} name={icon} class="" /> : null}
</a>
