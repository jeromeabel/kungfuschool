---
import { Icon } from 'astro-icon';

const { href, label, icon } = Astro.props;
---

<a href={href} aria-label={label} title={label} class="rounded-full bg-white p-2 text-black hover:bg-gold">
	<Icon name={icon} size={24} />
</a>
